export type DiscoveryInput = {
    segment: string
    symptom: string
    problem: string
  }
  
  export type DiscoveryOutput = {
    target: string
    problem: string
    insight: string
    opportunity: string
  }